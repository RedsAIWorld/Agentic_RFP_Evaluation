# Copy to .streamlit/secrets.toml for LOCAL convenience only (this file is
# gitignored and never read by the app's BYOK flow directly -- the app asks
# for a key in the sidebar every session). Not required to run the app.
# ANTHROPIC_API_KEY = "sk-ant-..."
# OPENAI_API_KEY = "sk-..."
